{
  "name": "remotion-lambda-template",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "remotion preview",
    "build": "remotion bundle src/index.tsx --public-dir=public --output=out",
    "deploy": "remotion lambda deploy",
    "render": "remotion lambda render"
  },
  "dependencies": {
    "@remotion/lambda": "^4.0.286",
    "remotion": "^4.0.134"
  },
  "devDependencies": {
    "@remotion/cli": "^4.0.286"
  }
}
